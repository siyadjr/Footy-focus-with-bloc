import 'package:flutter/material.dart';

class LeagueForTeams extends StatelessWidget {
  const LeagueForTeams({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
